sources:

  - source: [source, shell]
    doc: Test source 01
    actions:
      - shell: dir
      - shell: echo shell here

  - source: [source, shell, target]
    doc: Test source 02 with target and context variables
    actions:
      - shell: echo target=${target}
      - shell: echo name=${name}
      - shell: echo source=${source.sid}
